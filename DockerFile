# 1. Start with the slim Python base image
FROM python:3.12

# 2. Install uv directly from the official image
COPY --from=ghcr.io/astral-sh/uv:latest /uv /bin/

# 3. Set the working directory
WORKDIR /app

# 4. (OPTIMIZATION) Copy dependency files first to leverage layer caching.
COPY pyproject.toml uv.lock ./

# 5. (THE FIX) Explicitly create, activate, and then sync the environment.
# This is a more robust method that prevents Python path issues.
RUN uv venv && . .venv/bin/activate && uv sync --no-cache

# 6. Copy the rest of your application source code
COPY . .

# 7. Run the application using uv's modern run command
# `uv run` will automatically detect the .venv we created above.
CMD ["/app/.venv/bin/uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]

    


